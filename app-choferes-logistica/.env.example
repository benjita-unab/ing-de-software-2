# URL del backend NestJS (sin barra final recomendada).
# En dispositivo físico usa la IP LAN del PC o una URL de túnel (p. ej. trycloudflare).
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000

# Opcional: ruta por defecto para pruebas locales (ver uso en app).
EXPO_PUBLIC_RUTA_ID=
